# ⚡ Быстрый старт - Одна команда для развертывания

## 🎯 Копируйте и выполните на сервере:

```bash
cd ~/Lead_bot && git pull origin main && bash migrate_all.sh && systemctl restart leadbot && echo "✅ Готово!" && journalctl -u leadbot -n 30
```

---

## 📋 Или пошагово:

### 1️⃣ Обновить код:
```bash
cd ~/Lead_bot
git pull origin main
```

### 2️⃣ Выполнить миграции:
```bash
bash migrate_all.sh
```

### 3️⃣ Перезапустить:
```bash
systemctl restart leadbot
```

### 4️⃣ Проверить:
```bash
journalctl -u leadbot -f
```

---

## ✅ Что должно работать после миграции:

### В админ-панели (`/admin`):

✅ **📊 Статистика** - работает  
✅ **👥 Пользователи** - работает  
✅ **🎁 Лид-магниты**:
  - Добавление по URL ✅
  - Добавление файлом ✅ ← **НОВОЕ!**
  - Редактирование ✅
  - Удаление ✅

✅ **💰 Трипвайеры**:
  - Просмотр ✅
  - Редактирование ✅ ← **НОВОЕ!**
  - Добавление ✅ ← **НОВОЕ!**
  - Удаление ✅ ← **НОВОЕ!**

✅ **🔥 Прогрев**:
  - Просмотр сценариев ✅
  - Редактирование сценариев ✅ ← **НОВОЕ!**
  - Добавление сообщений ✅ ← **НОВОЕ!**
  - Удаление ✅

✅ **📢 Рассылки**:
  - Создание ✅
  - Отправка ✅
  - Редактирование ✅
  - Удаление ✅

✅ **👨‍💼 Администраторы** ← **НОВЫЙ РАЗДЕЛ!**:
  - Просмотр всех админов ✅
  - Добавление по TG ID ✅
  - Удаление из БД ✅

---

## 🐛 Если что-то не работает:

### Ошибка: "no such column: telegram_file_id"
```bash
bash migrate_all.sh
systemctl restart leadbot
```

### Ошибка: "no such table: admins"
```bash
python3 scripts/create_admins_table.py
systemctl restart leadbot
```

### Бот не отвечает:
```bash
systemctl status leadbot
journalctl -u leadbot -n 50
```

### Админ-панель не открывается:
```bash
# Проверьте ADMIN_IDS в .env
cat .env | grep ADMIN_IDS

# Добавьте свой ID если нужно
nano .env
# ADMIN_IDS=1670311707
```

---

## 📞 Контакты для поддержки

Если возникли проблемы:
1. Проверьте логи: `journalctl -u leadbot -f`
2. Проверьте БД: `sqlite3 leadbot.db ".tables"`
3. Пришлите логи для диагностики

---

## 🎉 После успешного развертывания:

Ваш бот теперь умеет:
- 📤 Отправлять файлы пользователям
- 👨‍💼 Управлять админами через интерфейс
- ✏️ Полностью редактировать сценарии и продукты
- 📊 Показывать детальную статистику
- 📢 Делать массовые рассылки

**Всё готово к работе!** 🚀
